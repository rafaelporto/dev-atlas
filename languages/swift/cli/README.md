# Swift — CLI & Terminal

> Building command-line tools in Swift — from a single `ParsableCommand` to git-style subcommand trees, powered by Apple's swift-argument-parser and compiled with SwiftPM.

---

| Article | Description |
|---|---|
| [Overview](overview.md) | Why Swift is a solid CLI language — native binaries, swift-argument-parser, SwiftPM executables — plus the state of TUIs in Swift |
| [Building CLIs](building-clis.md) | Hands-on: `ParsableCommand`, `@Argument`/`@Option`/`@Flag`, subcommands, validation, async, exit codes, and a testable structure |
