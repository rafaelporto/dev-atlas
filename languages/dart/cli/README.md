# Dart — CLI & Terminal

> Building command-line tools in Dart — from the `args` package to git-style `CommandRunner` apps compiled to self-contained native executables.

---

| Article | Description |
|---|---|
| [Overview](overview.md) | Why Dart makes a solid CLI language — native `dart compile exe`, the `args`/`mason` ecosystem — plus the state of TUIs in Dart |
| [Building CLIs](building-clis.md) | Hands-on: `ArgParser`, `CommandRunner` subcommands, native compilation, exit codes, streams, and a testable structure |
